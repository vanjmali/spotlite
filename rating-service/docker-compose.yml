services:
  rating-service:
    build:
      context: .
      additional_contexts:
        common-lib: ../common-lib
      dockerfile: ../Dockerfile.microservice
    environment:
      APP_PORT: 3000
      DB_HOST: rating-mongodb
      DB_PORT: 27017
      DB_USER: ${SRV_RATING_MONGO_USERNAME?}
      DB_PASS: ${SRV_RATING_MONGO_PASSWORD?}
      DB_NAME: ${SRV_RATING_MONGO_DATABASE?}
      CONTENT_GRPC_ADDRESS: ${SRV_CONTENT_GRPC_ADDRESS?}
      GRPC_PORT: ${SRV_RATING_GRPC_PORT?}
      NATS_URL: ${NATS_URL?}
      LOG_DIR: ${LOG_DIR?}
      NATS_URL: ${NATS_URL?}
      LOG_ROTATE_MAX_SIZE_MB: ${LOG_ROTATE_MAX_SIZE_MB?}
      LOG_ROTATE_MAX_BACKUPS: ${LOG_ROTATE_MAX_BACKUPS?}
      LOG_ROTATE_MAX_AGE_DAYS: ${LOG_ROTATE_MAX_AGE_DAYS?}
      LOG_ROTATE_COMPRESS: ${LOG_ROTATE_COMPRESS?}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318
    labels:
      traefik.enable: true
      traefik.docker.network: spotlite-api-gateway
      traefik.http.routers.rating-service.rule: PathPrefix(`/api/ratings`)
      traefik.http.services.rating-service.loadbalancer.server.scheme: https
      traefik.http.routers.rating-service.middlewares: rating-service-strip-prefix,rate-limit-global@file
      traefik.http.middlewares.rating-service-strip-prefix.stripprefix.prefixes: /api/ratings
      traefik.http.middlewares.rating-service-strip-prefix.stripprefix.forceslash: false
      traefik.http.services.rating-service.loadbalancer.server.port: "3000"
      traefik.http.services.rating-service.loadbalancer.healthcheck.path: /healthz
      traefik.http.services.rating-service.loadbalancer.healthcheck.interval: 5s
      traefik.http.services.rating-service.loadbalancer.healthcheck.timeout: 2s
      traefik.http.routers.rating-service.entrypoints: websecure
      traefik.http.routers.rating-service.tls: true

    depends_on:
      rating-mongodb:
        condition: service_healthy
    networks:
      - api-gateway
      - rating-internal

  rating-mongodb:
    image: mongo:8.2
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${SRV_RATING_MONGO_ROOT_USERNAME?}
      MONGO_INITDB_ROOT_PASSWORD: ${SRV_RATING_MONGO_ROOT_PASSWORD?}
      MONGO_INITDB_DATABASE: ${SRV_RATING_MONGO_DATABASE?}
      SRV_RATING_MONGO_USERNAME: ${SRV_RATING_MONGO_USERNAME?}
      SRV_RATING_MONGO_PASSWORD: ${SRV_RATING_MONGO_PASSWORD?}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mongosh --norc --quiet --username $$MONGO_INITDB_ROOT_USERNAME --password $$MONGO_INITDB_ROOT_PASSWORD --authenticationDatabase admin --eval 'db.runCommand({ ping: 1 })'",
        ]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      - rating-mongo-data:/data/db
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    networks:
      - rating-internal

networks:
  api-gateway:
    name: spotlite-api-gateway
  rating-internal:

volumes:
  rating-mongo-data:
